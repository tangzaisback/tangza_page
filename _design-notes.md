# 돌아온 탕자 — 디자인 분석 및 개선 기록

작성일: 2026-03-09

---

## 1. 현재 디자인 문제점

### 폰트
- **Orbit만 import됨** — CSS 상단에 Orbit만 Google Fonts로 불러오고 있고, Asta Sans는 import 없이 사용 중. 즉 Asta Sans는 브라우저 system font로 fallback되어 **본문 폰트가 의도대로 렌더링되지 않고 있음**
- **Orbit은 미래지향적·기하학적 폰트** — 개인 서사 중심의 글 블로그보다 테크/브랜딩 느낌에 가까움. "지적이고 차분하면서도 따뜻한" 분위기와 거리가 있음

### 색상
- **순수 흰색/검정** (`#ffffff`, `#000000`) — 너무 스테릴하고 차갑다. 약간의 온기가 없음
- 다크모드 배경 `#272727`도 중립적인 차가운 회색. 서재 같은 느낌이 부족함

### 레이아웃
- **Hero 섹션이 70vh** — 화면의 70%를 겨우 3줄짜리 텍스트가 차지함. 메인 콘텐츠(글/사진)로의 집중을 방해함
- **Writing.html의 2열 카드 그리드 + 가운데 정렬 텍스트** — 글 목록은 선형으로 스캔해야 자연스러움. 카드 가운데 정렬은 제목 비교가 어렵고 잡지적 느낌보다 앱 느낌에 가까움
- **스크롤 스냅** — 읽기 흐름을 강제로 끊어 오히려 거슬릴 수 있음
- **이중 헤더 레이아웃** (타이틀 위, 네비 아래 2단) — 세로 공간을 많이 사용하면서 정보 밀도가 낮음

---

## 2. 전체 디자인 컨셉 및 무드

**"사적인 문예지"** — 유학을 그만두고 돌아온 사람이 책상 위에서 쓰는 잡지. 화려하지 않지만 분명한 취향이 있고, 글이 전부인 공간.

레퍼런스: Craig Mod의 개인 블로그, Substack의 서재형 레이아웃, 오래된 문예지 레이아웃.
기하학적 정확함보다 인쇄물의 온기에 가깝게.

---

## 3. 색상 팔레트 (적용됨)

포인트 색상: **더스티 테라코타 B안** (`#c17a5a`)

### 라이트 모드

| 변수 | 값 | 역할 |
|------|----|------|
| `--bg-color` | `#faf9f6` | 따뜻한 오프화이트 |
| `--text-main` | `#1c1b19` | 따뜻한 거의-검정 |
| `--text-muted` | `#6e6c66` | 따뜻한 중간 회색 |
| `--text-date` | `#a09d97` | 연한 날짜 색 |
| `--border-color` | `#e5e2db` | 따뜻한 톤의 선 |
| `--hover-bg` | `#f3f1ec` | 호버 배경 |
| `--accent` | `#c17a5a` | 테라코타 액센트 |

### 다크 모드

| 변수 | 값 | 역할 |
|------|----|------|
| `--bg-color` | `#1a1917` | 따뜻한 거의-검정 |
| `--text-main` | `#e8e4dc` | 따뜻한 크림 |
| `--border-color` | `#2e2c29` | 따뜻한 계열 선 |

액센트 적용 위치: nav 호버 언더라인, 카테고리 탭 active 표시, 좋아요 버튼 liked 상태, 목록으로 돌아가기 hover

---

## 4. 폰트 조합 (적용됨)

**A안 — "문예지"**

| 용도 | 폰트 | 근거 |
|------|------|------|
| 헤딩 (포스트 제목, Hero 제목) | `Noto Serif KR` | 한국어 세리프. 인쇄물 같은 온기와 품격 |
| 본문 (포스트 본문, 요약, 부제) | `Noto Sans KR` | 읽기 최적화. 세리프 헤딩과 대비 선명 |
| UI 레이블 (날짜, 네비, 카테고리) | `Orbit` | 세리프 헤딩과 대비되어 특색 있음 |

```css
@import url('https://fonts.googleapis.com/css2?family=Noto+Serif+KR:wght@400;700&family=Noto+Sans+KR:wght@400;500&family=Orbit&display=swap');
```

본문 타이포 수치:
- 폰트 크기: `16px` → `17px`
- 줄간격: `normal` → `1.85`
- 포스트 제목: `1.4rem Orbit` → `1.9rem Noto Serif KR`

---

## 5. 레이아웃 개선 (적용됨)

### 헤더
- 2단 (타이틀 위 / 네비 아래) → **1단 수평** (타이틀 좌, 네비 우, 높이 64px)

### Hero 섹션
- `min-height: 70vh` → `35vh`

### 포스트 뷰
- 제목 좌측 정렬 (center → left)
- 제목 아래 `border-bottom` 구분선 추가
- 제목 크기 1.4rem → 1.9rem

### Writing.html 목록
- 2열 카드 그리드 (`post-card` / `post-grid`) → **단일 컬럼 리스트** (`post-item` / `writing-list`)
- 제목 좌측, 날짜 우측 정렬
- 카드 테두리 제거, 구분선만 사용 (더 가볍고 인쇄물에 가까움)

---

## 6. 적용 우선순위 (실제 진행 순서)

1. ✅ 폰트 import 교체 (`_layouts/default.html`)
2. ✅ CSS 변수 색상 팔레트 전면 교체 (`assets/css/style.css`)
3. ✅ 헤더 1단 수평 레이아웃
4. ✅ 타이포그래피 전체 (헤딩/본문 폰트 교체)
5. ✅ 포스트 뷰 (제목 크기 + 좌측 정렬)
6. ✅ Hero 섹션 축소
7. ✅ Writing.html 목록 레이아웃 변경
8. ✅ 본문 `word-break: keep-all` 적용 (단어 단위 줄바꿈)
